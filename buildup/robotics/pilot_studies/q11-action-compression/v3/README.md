# Q11 revision v3

The [parent study](../README.md#bounded-physical-pilot-v3-preparation) owns the protocol explanation,
precheck evidence, exact commands, output paths and current status. This folder owns the frozen
v3 executable protocol and calibration snapshot. `run.sh` is prepared; no v3 episode has run.
The immutable workspace image, original collector and binary codecs are reused with provenance.
